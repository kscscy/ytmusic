<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="java76.pms.dao.MusicDao">

  <resultMap type="music" id="musicMap">
    <id 		column="bno" 		property="no"/>
    <result column="cre_dt" 		property="createdDate"/>
    <result column="afile" 		property="attachFile"/>
  </resultMap>

  <select id="selectList" resultMap="musicMap" parameterType="map">
    select 
      bno,
      title,
      views,
      cre_dt 
    from music
    order by 
    <choose>
      <when test="keyword == 'title'">title </when>
      <when test="keyword == 'views'">views </when>
      <otherwise>bno </otherwise>
    </choose>
    <choose>
      <when test="align == 'asc'">asc</when>
      <otherwise>desc</otherwise>
    </choose>
    limit #{startIndex}, #{length}
  </select>
  
  <insert id="insert" parameterType="music">
    insert into music(title,content,pwd,cre_dt,afile) 
    values(#{title},#{content},#{password},now(),#{attachFile})
  </insert>
  
  <delete id="delete" parameterType="map">
    delete from music 
  	where bno=#{no} and (pwd is null or pwd=#{password})
  </delete>
  
  <update id="update" parameterType="music">
    update music set 
      title=#{title},
      content=#{content},
      cre_dt=now(),
  	  afile=#{attachFile}
    where bno=#{no} and (pwd is null or pwd=#{password})
  </update>
  
  <select id="selectOne" resultMap="musicMap" parameterType="int">
    select 
      bno,
      title,
  	  content,
      views,
      cre_dt,
  	  afile 
    from music
    where bno=#{no}
  </select>
</mapper>






